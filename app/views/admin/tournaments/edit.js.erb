$("#dialog_update_tournament")
  .html("<%= j render('form') %>")
  .dialog("option", "title", "Update Tournament")
  .dialog("open");
$("#tournament_start").datepicker({
    dateFormat: 'yy-mm-dd',
    autoSize: true
});
$("#tournament_finish").datepicker({
    dateFormat: 'yy-mm-dd',
    autoSize: true
});
