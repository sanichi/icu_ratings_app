<% if @tournament.errors.size == 0 %>
  $("#show_stage").html("<%= escape_javascript(t(@tournament.stage)) %>");
  $("#dialog_update_tournament").dialog("close");
<% else %>
  alert("<%= escape_javascript(render('shared/error', object: @tournament, title: 'Error updating stage:')) %>")
<% end %>
