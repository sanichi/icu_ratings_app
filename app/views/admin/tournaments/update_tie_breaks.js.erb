<% if @tournament.errors.size == 0 %>
  $("#show_tie_breaks").html("<%= escape_javascript(@tournament.tie_breaks) %>");
  $("#dialog_update_tournament").dialog("close");
<% else %>
  alert("<%= escape_javascript(render('shared/error', :object => @tournament, :title => 'Error updating tie breaks:')) %>")
<% end %>
