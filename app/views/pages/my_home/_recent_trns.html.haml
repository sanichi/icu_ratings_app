%table{class: "cushioned"}
  %tr
    %th{colspan: 3} Recent Tournaments
  - recent_trns.each do |player|
    %tr
      %td= link_to player.tournament.name_with_year, player
      %td{class: "centered"}= player.new_rating
      %td{class: "righted"}= sign player.rating_change
  - if recent_trns.size < max
    - (recent_trns.size..max-1).each do |t|
      %tr
        %td{colspan: 3} &nbsp;
      