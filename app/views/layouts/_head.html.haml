%title= (yield :title).presence || "ICU Ratings"
= stylesheet_link_tag :all
= javascript_include_tag :defaults
= csrf_meta_tag
= yield :head
- if content_for? :javascript
  = javascript_tag do
    = yield :javascript
